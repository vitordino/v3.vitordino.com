import { default as Text } from './TextSlice'
import { default as Quote } from './QuoteSlice'
import { default as Image } from './ImageSlice'

const Slices = {
	Text,
	Quote,
	Image,
}

export default Slices
